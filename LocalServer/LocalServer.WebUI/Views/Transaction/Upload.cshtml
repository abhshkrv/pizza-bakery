@{
    ViewBag.Title = "Upload";
}

<h2>Setup</h2>
@using (Html.BeginForm("Setup", "Transaction", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input type="file" name="file" id="file" />
    <input type="submit" value="submit" />
}
